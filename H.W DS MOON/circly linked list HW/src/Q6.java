public class Q6 {
    //{لتنفيذ طريقة clone() في فئة CircularlyLinkedList، يجب أن نقوم بإنشاء قائمة جديدة تحتوي على نفس العناصر الموجودة في القائمة الأصلية. القائمة الجديدة
    // يجب أن تكون مستقلة وغير متصلة بالقائمة الأصلية، وذلك بإنشاء عقدة جديدة لكل عنصر في القائمة الأصلية وتوصيلها بشكل منفصل.

     //  وفيما يلي الخوارزمية لتنفيذ طريقة clone():

       //1. قم بإنشاء قائمة جديدة فارغة لتخزين العناصر المستنسخة.

       // 2. إذا كانت القائمة الأصلية فارغة، فقم بإرجاع القائمة الجديدة الفارغة.

       // 3. قم بإنشاء عقدة جديدة لكل عنصر في القائمة الأصلية وقم بتعيين العنصر في العقدة الجديدة.

        //4. قم بتوصيل العقدات الجديدة ببعضها البعض لإنشاء قائمة متصلة جديدة.

        //5. قم بتعيين الرأس والذيل للقائمة الجديدة بناءً على العقدة الأولى والعقدة الأخيرة.

       // 6. قم بإرجاع القائمة الجديدة.

        //وفيما يلي تنفيذ الخوارزمية في شكل الكود:

    //java
        public CircularlyLinkedList<E> clone() {
        CircularlyLinkedList<E> newList = new CircularlyLinkedList<>();

        if (isEmpty()) {
            return newList;
        }

        Node<E> currentNode = head;

        // إنشاء عقدة جديدة لكل عنصر وتوصيلها ببعضها البعض
        do {
            newList.addLast(currentNode.getElement());
            currentNode = currentNode.getNext();
        } while (currentNode != head);

        return newList;
    }


       // باستخدام هذه الخوارزمية، يمكننا تنفيذ طريقة clone() في فئة CircularlyLinkedList. يتم إنشاء قائمة جديدة تحتوي على نفس العناصر الموجودة في القائمة الأصلية، وتكون مستقلة عنها.
    }

}
